<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= title %></title>
  <style>
    /* ------------------------------
       GLOBAL STYLES
    ------------------------------- */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: Arial, sans-serif;
      background-color: #181818;
      color: #ffffff;
      line-height: 1.6;
    }

    a {
      text-decoration: none;
      color: inherit;
    }

    /* ------------------------------
       HEADER
    ------------------------------- */
    header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      background-color: #000;
      padding: 20px;
    }

    .logo img {
      height: 40px;
    }

    nav ul {
      list-style: none;
      display: flex;
      gap: 20px;
    }

    nav a {
      color: #fff;
      font-weight: 500;
      transition: color 0.2s ease;
    }

    nav a:hover {
      color: #ff9900;
    }

    /* ------------------------------
       HERO SECTION
    ------------------------------- */
    .hero {
      text-align: center;
      background-color: #000;
      padding: 60px 20px;
    }

    .hero h1 {
      font-size: 2.2rem;
      margin-bottom: 20px;
    }

    .hero button {
      background-color: #ff9900;
      border: none;
      color: #000;
      font-weight: bold;
      padding: 12px 24px;
      cursor: pointer;
      border-radius: 4px;
      transition: background-color 0.2s ease;
      margin-bottom: 30px;
    }

    .hero button:hover {
      background-color: #ffc966;
    }

    .categories {
      display: flex;
      justify-content: center;
      gap: 20px;
      margin-top: 20px;
    }

    .categories a {
      color: #ff9900;
      font-weight: 600;
      padding: 6px 12px;
      border: 1px solid #ff9900;
      border-radius: 4px;
      transition: background-color 0.2s ease, color 0.2s ease;
    }

    .categories a:hover {
      background-color: #ff9900;
      color: #000;
    }

    /* ------------------------------
       CONTENT SECTION
    ------------------------------- */
    .content {
      display: flex;
      gap: 20px;
      padding: 20px;
      max-width: 1200px;
      margin: 0 auto;
    }

    .main-content {
      flex: 3;
      background-color: #2e2e2e;
      min-height: 400px;
      border-radius: 4px;
      padding: 20px;
    }

    .sidebar {
      flex: 1;
      background-color: #3a3a3a;
      min-height: 400px;
      border-radius: 4px;
      padding: 20px;
    }

    /* ------------------------------
       FEATURED VIDEOS SECTION
    ------------------------------- */
    .featured-videos {
      max-width: 1200px;
      margin: 40px auto;
      padding: 0 20px;
    }

    .featured-videos h2 {
      font-size: 1.5rem;
      margin-bottom: 20px;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .video-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 20px;
    }

    .video-item {
      background-color: #2e2e2e;
      border-radius: 4px;
      padding: 10px;
      min-height: 150px; /* Placeholder height */
      display: flex;
      align-items: center;
      justify-content: center;
    }

    /* ------------------------------
       FOOTER
    ------------------------------- */
    footer {
      background-color: #FF9600;
      padding: 20px;
      text-align: center;
    }

    .footer-social {
      margin-bottom: 10px;
    }

    .footer-social a {
      margin: 0 10px;
      transition: transform 0.2s ease;
    }

    .footer-social a:hover {
      transform: scale(1.1);
    }

    /* Social icon images styling */
    .social-icon {
      height: 24px;
      width: auto;
      vertical-align: middle;
    }

    .footer-copy {
      font-size: 0.9rem;
      color: #F9F6F2; /* Changed to a white-bones color */
    }

    /* ------------------------------
       RESPONSIVE EXAMPLE
    ------------------------------- */
    @media (max-width: 768px) {
      .content {
        flex-direction: column;
      }
    }
  </style>
</head>
<body>

  <!-- HEADER -->
  <header>
    <div class="logo">
      <!-- Replace with your logo -->
      <img src="logo.png" alt="Pergerakan Physicist Society" />
    </div>
    <nav>
      <ul>
        <li><a href="#">About</a></li>
        <li><a href="#">Activity</a></li>
        <li><a href="#">Article</a></li>
        <li><a href="#">Topics</a></li>
      </ul>
    </nav>
  </header>

  <!-- HERO SECTION -->
  <section class="hero">
    <h1>FISIKA ITU SIMPEL?<br/>Mari kita bahas di sini!</h1>
    <button>Lihat Konten</button>
    <div class="categories">
      <a href="#">Astrofisika</a>
      <a href="#">Fisika Inti</a>
      <a href="#">Termodinamika</a>
    </div>
  </section>

  <!-- CONTENT SECTION -->
  <section class="content">
    <!-- Main Content -->
    <div class="main-content">
      <!-- Replace with your actual content -->
      <h2 style="margin-bottom: 10px;">Main Content</h2>
      <p>Placeholder area for articles, images, or any primary content you want to display.</p>
    </div>

    <!-- Sidebar -->
    <aside class="sidebar">
      <h3 style="margin-bottom: 10px;">Sidebar</h3>
      <p>Placeholder for additional info, recent posts, or other widgets.</p>
    </aside>
  </section>

  <!-- FEATURED VIDEOS SECTION -->
  <section class="featured-videos">
    <h2>FEATURE VIDEOS</h2>
    <div class="video-grid">
      <!-- Example placeholders for videos -->
      <div class="video-item">Video 1</div>
      <div class="video-item">Video 2</div>
      <div class="video-item">Video 3</div>
      <div class="video-item">Video 4</div>
    </div>
  </section>

  <!-- FOOTER -->
  <footer>
    <div class="footer-social">
      <!-- Social links with logo icons -->
      <a href="#" aria-label="YouTube">
        <img src="youtube-logo.png" alt="YouTube" class="social-icon" />
      </a>
      <a href="#" aria-label="TikTok">
        <img src="tiktok-logo.png" alt="TikTok" class="social-icon" />
      </a>
      <a href="#" aria-label="Instagram">
        <img src="instagram-logo.png" alt="Instagram" class="social-icon" />
      </a>
    </div>
    <div class="footer-copy">
      © 2023 Padjadjaran Physicist Society
    </div>
  </footer>

</body>
</html>
